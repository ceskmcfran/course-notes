# Preparando el API para produccion

Para que nuestra API sea accesible desde cualquier lugar debemos añadir el middleware `cors` a `express`, primero debemos instalarlo con el siguiente comando: `npm i cors`

# Clientes

- Apollo
